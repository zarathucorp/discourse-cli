{
  "name": "discourse-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "discourse-cli": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/fix-bin.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.8.3"
  }
}
